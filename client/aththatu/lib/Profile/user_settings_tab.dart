import 'package:flutter/material.dart';

class UserSettingsTab extends StatelessWidget {
  const UserSettingsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('User Settings Content'),
    );
  }
}